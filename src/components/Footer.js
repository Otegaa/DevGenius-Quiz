import Quit from './Quit';
import NextButton from './NextButton';

const Footer = () => {
  return (
    <footer className="footer">
      <Quit />
      <NextButton />
    </footer>
  );
};
export default Footer;
