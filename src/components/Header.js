import { Logo } from './Logo';
import Timer from './Timer';

const Header = () => {
  return (
    <header className="header">
      <Logo />
      <Timer />
    </header>
  );
};
export default Header;
